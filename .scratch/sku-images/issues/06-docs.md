# 06: 文档修订——README 与交换集口径

**What to build:** 文档对齐实现。README「商品主图资产」节扩成「商品主图与 SKU 图」：主图三条口径 + 「SKU 图同规」+ 代填与来源三态 + 重试入口（同一条 CLI，对象为 SKU 图流水行，含可照抄的命令示例）；「数据落在哪」等节补新表与量级说明；交换集/包格式相关表述对齐七表与 v2「六表」字样全文清理。CONTEXT.md 本线已改的两条（「商品主图」「SKU 图」与交换集七表）复核无漂移。ADR-0045 状态随全线落地维护（转「已接受并实现」的条件与判据写进票面 Comments）。

参照：规格实现决策 7、用户故事 15–17。

**Blocked by:** 票 05（已落地，见 [ticket-plan](../ticket-plan.md) 实施进展）

**Status:** resolved

- [x] README 相应小节与程序实际行为逐句核对（含重试命令示例可照抄即用）
- [x] 全文「六表」类过时表述清干净（对齐七表与 v2）
- [x] CONTEXT.md 两条术语复核无漂移
- [x] ADR-0045 状态与落地记录更新

## Comments

- 2026-09-24 落地（文档票：只动文档，程序行为零改动）。逐处实况：
  - **README「商品主图与 SKU 图」**（原「商品主图资产」；全仓只它自己一处引用，改名安全）：
    主图三条口径原文保留（只加「**商品主图**：」领句与断行微调）；SKU 图按规格实现决策 7 与
    用户故事 15–17 写——详情页规格里配的颜色缩略图、一个 SKU 至多一张；与库存行同源解析、
    不多开页面；每次真实观测重读重存；下载失败不挡库存提交、缺图不借用相邻观测；页面没配图的
    （颜色随机型规格、单规格商品）以**当次观测的商品主图**代填，当次主图也缺就记无图行，
    专属图 / 主图代填 / 无图三态在 `sku_image_versions.source` 里可区分；空图不是失败、不进
    周报待办；每个 SKU 每次观测一行流水（含失败与无图行），字节同样进 `product_image_assets`
    池；量级每观测每 SKU 一行、包内每周万行级。
  - **README 重试入口**：与主图并排两条命令、示例照抄即用；「两参数互斥、必给一个」「对象必须
    是失败行、且仍是该对象最新的记录（之后已有更新的记录就拒绝）」按 argparse 与
    `db.retry_sku_image` 的实况写。**04 审查留档那条**（待办只报条数、编号出处归 README）落在
    SKU 图重试那句的括注：流水行编号取自 `sku_image_versions.id`，周报待办只报失败条数、不点
    行号。**「数据落在哪」**在库存库那一带加一行：字节池 + 两张观测流水（含量级）。
  - **ops 文档**（`docs/ops/多机分片采集机制.md`）：54 行「交换集六表（见 §7）」改「七表
    （见 §6）」＋`format_version` 现为 `v2`＋计数键补 `rows_sku_image_versions`；新增一条
    「格式版本与兼容方向」按 ADR-0045 写双向兼容（`v1` 旧包照收、SKU 图这半为无；原六张表维持
    硬要求；旧程序读 `v2` 新包，多出的表与元数据键被忽略）。182 行改七表并把
    `sku_image_versions` 加进枚举。
  - **判断项一（做了）**：§6 观测表一组之后加「**SKU 图流水**」一组——先查后写（键含
    `COALESCE` 的 NULL 折叠）、不依赖唯一索引在不在（老库上它可能没建起来）、各机观测天然互异
    无裁决（既不进取胜方账也不进冲突账）、`v1` 包缺席=这半为无。内容照
    `merge._merge_sku_image_versions` 与 `db.SKU_IMAGE_DEDUPE_KEY` 的实况写。
  - **判断项二（做了）**：§8 验收「各表有数」加半句——不含 `sku_image_versions`（历史补齐阶段
    它本来就可能为空，判据在 `tools/acceptance_check.py` 的核心表集）；它的图字节照归「按清单
    补齐」那一半核。与 `_CORE_TABLES` 有意不含它对得上。
  - **票面没点名、同一处顺手改的两处（都在本 ops 文档内）**：① 验收上一行「承载取胜结果的
    **五张表**」随本线变六张（票 05 已把 `sku_image_versions` 加进收敛对照 `_COMPARE_TABLES`）；
    ② 54 行「（见 §7）」指向错节（§7 是上机步骤编号；交换集在 §6 定义），一并改指 §6。
  - **判断项三（做了，ADRs）**：ADR-0045 决策节两处「六表」措辞改「六张表」（同义，与 0039 的
    用词一致），全树 `grep 六表` 遂为零、不留叙述性残留；ADR-0039 状态行按仓里惯例（状态行加
    指路、不改正文）补一句后注指到 0045。
  - **CONTEXT.md**：两条词条（「商品主图」「SKU 图」）与交换集七表逐一复核无漂移（对规格术语行
    与 `parse.extract_main_image`、`db.SKU_IMAGE_*` 的实况），随本票落库——此前它是共享文件、
    叠着分析页线在途词条没敢提交，那边已落 `e250f86`，`git diff` 里剩下的就是本线这三处。
  - **命令核对**：示例照抄——`--help` 退出码 0；两参数同给报 `not allowed with`、都不给报
    `one of the arguments ... is required`（都退出 2）；用一次性临时库
    （`.scratch/sku-images/ticket06-run/throwaway.db`，**不是生产库**）照命令形实跑到 db 层，
    守卫按预期拒（`只能重试失败的 SKU 图版本`）。
  - **「六表」清零**：现行文档（README、CONTEXT.md、docs/ops/、AGENTS.md）`grep 六表` 为零；
    历史 ADR 正文的「六张表」按惯例不动（0039 只加状态行后注）。
  - **ADR-0045**：状态转「已接受并实现（2026-09-24；规格与票面见 `.scratch/sku-images/`，落地
    实况见文末）」；「落地（分票）」补各票提交号（01 `7365a2a`；02 `6943400`＋`cf2461d`；
    03 `3ca2924`＋`27583cb`；04 `5b6aa10`＋`0da825d`；05 `2c5163f`＋`c969040`；06 随该票落库）。
    转状态判据：01–05 票面全部 resolved、最新全量 **1366 项、7/7 段 OK**（票 05 的 final 那趟）。
  - **验收**：隔离副本（基线 `c969040`＋本票改动）分段全量 **`合计 1366 项（预期 1366，计数
    对账一致），总耗时 759.8s，段结果 7/7 段 OK，结论 OK`**（日志
    `.scratch/logs/tickets/sku-images/06/full-suite/`）。跑完时 trunk 已落到另一会话的
    `a3f0e32`（世界模板试点：只动 `test_exchange` 夹具、整线 +4 项用例，其自身全绿），为让记录
    贴住本笔提交的父提交，在 `a3f0e32`＋本票改动上又跑一趟：**`合计 1370 项（预期 1370，计数
    对账一致），总耗时 744.0s，段结果 7/7 段 OK，结论 OK`**（日志
    `.scratch/logs/tickets/sku-images/06/on-a3f0e32/`）。
  - **留给人工/后续**：`docs/ops/三机上机清单.md` 的第 299/331/333 行是同本 ops 文档 §8 同一句
    话的副本（「各表有数」「五张表」），随本线也该对齐——但不在本票提交白名单（只走 README／
    本 ops 文档／CONTEXT／ADR-0045／本线票与总览），留一笔给人工或下张票。
