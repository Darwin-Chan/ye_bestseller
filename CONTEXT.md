# 1688 库存快照

本项目采集固定 1688 店铺的商品榜单与 SKU 库存快照，用于形成可追溯的库存观测数据。

## Language

**店铺**：配置中参与采集的 1688 商家范围。避免使用“账号”指代店铺；账号是登录身份。

**商品**：榜单中的一个 offer，由店铺范围内的商品编号识别。避免把 SKU 当作商品。

**SKU**：商品下的一条库存行，包含规格、价格和可售库存。多规格商品由 SKU 编号识别；单规格商品没有规格区分、平台也不分配 SKU 编号，整件商品按一条 SKU 行记录。

**单规格商品**：平台不使用 SKU 交易、详情页没有规格明细的商品；它只有一件默认规格，可售库存是整件商品级的。

**库存快照提交**：把商品的一次完整采集结果作为整体保存，并清除该商品在本轮的旧失败记录。同一轮、同一店铺、同一商品再次提交完整结果时，后一次结果替换相同 SKU 的前一次结果；新 SKU 追加，本次未出现的旧 SKU 保留。若本次观测的粒度与既有记录不同（单规格与多规格之间切换），提交时清除该商品本轮快照与当天库存里的相反粒度记录，更早日期的历史观测保留。

**成功库存快照**：由轮次、店铺、商品和 SKU 共同识别；同一识别组合最多保留一条成功记录。失败记录和跳过记录不改变成功库存快照的身份。

**榜单批次**：店铺列表中一个可识别的分页或“加载更多”结果单元。前 30 个榜单批次是当前上限（`config/config.toml` 的 `max_pages_per_shop`）。

**榜单状态**：该店本轮榜单的完整程度，只有三态。

- `待处理`：本轮还没轮到这家店。
- `失败`：尝试过，但没拿到完整榜单。列表加载失败、店铺 deny 跳店、整轮 deny 中止、跨天中止、人工介入超时和详情预算耗尽都归这一态，原因写在备注里。
- `完成`：拿到了完整榜单。

**残缺榜单**：店铺在榜单阶段被打断时，本轮在该店**已发现**的商品集合。它不是该店榜单的完整快照，只保证「本轮发现过的商品都有榜单行」。与完整榜单相对：完整榜单在整店遍历结束时整体替换该店本轮榜单行，残缺榜单只增不减——续跑再次中断也不会抹掉先前发现的行。榜单状态为 `失败` 且已发现商品数大于零时，该店就是残缺榜单。

**轮次**：一次按固定店铺范围启动的采集过程，严格绑定一个北京时间日期。

**轮次日期**：轮次启动时的北京时间日期。日期比较只用它，不用 UTC 日期，也不用采集时刻反推。

**轮次店铺范围**：轮次启动时确定的店铺集合，连同各店的名称与地址一起固定下来。续跑以这一份为准，不因为配置里增删店铺而改变。

**续跑**：对同一轮次的继续采集，只在该轮次仍进行中、且日期与店铺范围都不变时发生。

**轮次终态**：轮次停止继续采集的原因，例如完成、跨天中止、失败率超限、详情预算耗尽、整轮 deny 超限、人工放弃；与「进行中」相对。终态一旦写入不再改变：同一终态的重复收尾可以忽略，不同终态不得覆盖。

**驱动**：一轮采集中操作浏览器的实现方式。当前只有一种：用普通进程拉起浏览器，由 Playwright 经调试端口接管，榜单靠点击商品卡片枚举、详情逐个进入。配置里的 `driver` 只接受 `pw_cdp`。

**界面实例**：一次打开的控制界面。同一时刻至多一个：再次启动不建立第二个界面，而是提示已有窗口后退出。界面可以关掉，采集不受影响。

**采集进程**：正在执行一轮采集的进程，可以是界面拉起的子进程，也可以是命令行直接运行的同一入口。同一时刻至多一个：由它启动时持有的锁保证。它消失（暂停、崩溃、被人工中止）之后就不再算「有任务在跑」，同一轮次可以再次启动续跑。

**停止请求**：界面在采集进程运行中写下的「请停下」意图，用于暂停。它按目标进程识别（PID 与启动时刻），由采集进程在检查点或长睡眠的间隙认领：认领时回写一次「已收到」（界面据此把等待放宽到收尾窗口），进程退出时把它删掉。对不上身份的请求一律无视，所以新起的进程与命令行续跑不会被旧请求影响。停止请求不是轮次终态。

**协作停止**：采集进程自己读到停止请求（或轮次已有终态）后，在下一个检查点退出，并执行自己的收尾：关掉本次启动的浏览器、清掉身份行、释放采集锁。它与「强制停止」相对。

**强制停止**：协作停止没有在窗口内发生时，界面强制结束采集进程，并按调试端口的归属收尾本任务启动的浏览器。窗口为 8 秒；采集进程回写「已收到」之后再给 10 秒。

**界面刷新**：界面从数据库取一次当前状态并重画一次。三个取数入口各算一次刷新：开始页取数、过程页取数、结果页取数。「轮询」指调度——每约 2 秒安排下一次刷新；「刷新」指那一次取数本身。

**在途刷新**：已经开始、还没返回的那一次界面刷新。同一时刻至多一条：上一次刷新回来之后才排下一次；手动触发的刷新（暂停、继续、切页签）排在它后面，取到的是新数据，而不是复用上一次的结果。

**界面会话事实**：界面自己记着的、既不是数据层事实也不是采集进程身份的那几件：本界面认领的轮次编号、是否被用户按下暂停、停止窗口的进展、本次已抓时长，以及「子进程被拒」这类只对本次会话成立的事。避免用「会话状态」指代它——本项目的「会话」指 Windows 会话（见 ADR-0008）。

**连接即迁移**：界面每次开数据库连接都会重跑建表与迁移（`connect()`）。所以迁移里任何整表动作都要自带「这个库还缺它」的条件，否则每次刷新都要付一遍随数据量增长的成本。

**迁移报告**：一次开库跑过的迁移动作——哪些段真正动过手，以及整表动作的明细（这次删了几行重复快照、建了哪些索引）。测试与基准读它来判断「这次开库付了什么成本」，不再按 SQL 文本猜。

**同日去重**：同一店铺中，当前北京时间日期已有一次完整成功详情观测的商品默认跳过详情访问；跳过、失败和不完整库存观测不构成同日去重证据。

**跳过**：本轮不访问详情的已处理结果，由同日去重或同名暂缓产生；跳过计入已处理、不计入失败，也不构成库存证据。

**补采**：对没有完整成功详情观测的失败、不完整库存观测或同店同名商品执行的再次详情采集；已知商品编号优先于商品名判断。

**详情观测**：对某个商品取一次详情页的结果。成功提交后成为成功库存快照；「不完整库存观测」「失败」「跳过」都是它的结果。同日去重、补采与本轮尝试额度都以它为单位判断。

**不完整库存观测**：商品详情没有给出全部库存行的可售量，或没给出可售量的观测。它不构成同日去重证据，按可重试失败处理。

**详情预算**：一轮中允许进入商品详情流程的商品机会数；同日跳过不消耗预算，同一商品的重试属于同一次机会，点击采集、同名补采和失败补采共享预算。

**详情机会**：商品在一轮中进入一次详情流程的名额，由同店同商品共用；同一商品的重试复用同一个机会，同日跳过不占机会。

**详情预算耗尽**：详情预算已全部消耗但仍有待补采商品时的轮次终态；已写入数据保留，未处理商品留待下一轮重新发现。

**库存差分**：同一店铺、商品和 SKU 的当前日期库存减最近更早日期库存，表示观测到的净变化，不等同于实际销量。差分基准只取同一 SKU 的历史行，因此单规格与多规格之间的切换不会互相比较。

**失败率**：本轮商品详情失败与点击后未得到商品结果相对于尝试总数的比例。超过配置阈值时本轮结束并进入人工处理状态。

**本轮计数**：一轮采集中「发现了多少商品、处理了多少、成功了多少」的那几个数，由
`Database.round_tally()` 一处算出（见 [ADR-0017](docs/adr/0017-round-counts-in-one-place.md)）。
商品数一律按榜单行去重；跳过算已处理、不算失败；成功商品数与成功 SKU 行数只数榜单行里
有的商品。「有快照、无榜单行」的商品是**孤儿**（IS-49），单列一个数，不算成功商品。失败
率是它的用法之一，算式本身不算在它里面。

## Rules

- 本期验收范围为单账号、2 家店、每店最多 30 个榜单批次；实际可用批次少于 30 个时按实际数量完成。
- 采集驱动只有一种（见 [ADR-0010](docs/adr/0010-single-crawl-driver-pw-cdp.md)）；配置的 `driver` 只接受 `pw_cdp`，写了别的值在启动时报错，不静默换路径。
- 轮次店铺范围在启动时确定，续跑不得增删店铺。
- 一轮跨到北京时间日期边界时结束；跨日后新建轮次，不续跑旧轮次。
- 完整库存快照提交后，若当前北京时间达到截止线，保留该商品数据并停止本轮；停止判定不回滚已提交数据。
- 进行中的轮次只接受同日期、同店铺范围的续跑；跨日启动时先把旧轮按跨天终态收尾再新建轮次，同日换店铺范围则拒绝，不并入该轮。
- 达到截止线或已进入次日时不再开始新的商品详情采集；本轮已提交的数据保留。
- 同一时刻至多一个界面实例、至多一个采集进程；「有任务在跑」以采集进程是否存在为准，不以轮次是否进行中为准（暂停后的轮次仍在进行中，但已经没有采集进程）。
- 同一时刻至多一条在途刷新：轮询在它回来之后才排下一次，手动刷新排在它后面。刷新慢只会让界面显示得晚，不会堆成积压。
- 关闭界面不打断正在跑的采集；再次打开界面要能看到那个采集进程并中止它。
- 「暂停」＝写下停止请求，轮次保持进行中、可续跑；「中止」＝先把轮次收尾为人工放弃，再停止进程。两者都先请求协作停止，窗口内没停下才强制停止。
